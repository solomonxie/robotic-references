- name: Set hostname
  ansible.builtin.hostname:
    name: "{{ inventory_hostname }}"

# The R2S's job in mycar isn't decided yet (see mycar/README.md) -- this role
# is currently just base_setup + hostname. Add board-specific tasks here once
# its role is picked (e.g. dedicated WiFi AP, VPN/reverse-proxy node).
